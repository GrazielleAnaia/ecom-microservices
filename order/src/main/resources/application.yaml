spring:
  application:
    name: order-service
  #PostgreSQL
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: graziuser
    password: grazipassword
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8083